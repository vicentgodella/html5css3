article
  mixin labHeader('Semantics: semantics elements')

  h3 Duration
  p 15 minutes

  h3 Description
  p Starting with the provided page, adapt it to use schema.org:
  ul
    li Convert the provided markup for a blog post to semantic HTML5 using <b><code>section</code></b>, <b><code>nav</code></b>, <b><code>header</code></b> and <b><code>article</code></b>. Also, transform any <code>h2</code> tags into <code>h1</code>.
    li Update the CSS so that the layout still works in Chrome and Firefox. Add JavaScript to make it work in IE.
    li Check the resulting page structure using <a href="http://gsnedders.html5.org/outliner/">HTML5 Outliner</a>.